version: "3.8"
services:
  frontend:
    build: .
    container_name: "DKPFrontEnd"
    env_file:
      - .env
    Expose: '5174'
    volumes:
      - .:/app
      - "/app/node_modules"
    networks:
      - common
